# Required offline Course 1 dependency set, verified 2026-07-28.
# Exact pins make a clean learner installation reproducible. The setup also
# records the complete installed dependency tree with `pip freeze`.
pytest==9.0.2
